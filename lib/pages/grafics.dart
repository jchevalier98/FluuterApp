import 'package:flutter/material.dart';
import 'package:navigation_bar/util/graph_widget.dart';

class Grafics extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 250,
      child: GraphWidget(),
    );
  }
} 